import java.time.*;
class Solution {
    public String dayOfTheWeek(int day, int month, int year) {
        LocalDate date  = LocalDate.of(year,month,day);
        String str = date.getDayOfWeek().name();
        str = str.substring(0,1)+str.substring(1).toLowerCase();
        return str;
    }
}
