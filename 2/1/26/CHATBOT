import re

def chatbot():
    while True:
        user = input("You: ")
        if "hello" in user.lower():
            print("Bot: Hi there!")
        elif re.search(r"bye", user.lower()):
            print("Bot: Goodbye!")
            break
        else:
            print("Bot: I don’t understand.")

chatbot()
